## [进行中]

## [已完成]

- 2026-09-04：完成 `snn_op → snn_exec`。新增 `!snn.voltage<T>`、`!snn.state<...>`，以及 `snn_exec.generic/state/sw/ss/mul/reduce/integrate/fire/yield` 和 `--lower-snnop-to-snnexec`。lowering 输入为 fusion+DCE 后 IR，覆盖全部现有 fused op；保留 module-level `snn_op.param` symbol，不嵌入权重。generic 内 population/state/voltage shape 删除 time_dim，外部结果维持原 shape；只生成 DCE 后存活的 spike/tracer。NIR importer 现在按原生 dtype 写出 threshold、tracer bounds 与 voltage dtype，实际 block_00 为 voltage/threshold i16、tracer i8。重新生成 `output/block_00_split_residual_norm.snnexec.mlir`，得到 13 generic、10 sw、3 mul、2 ss；最终 parse/verify/print 通过；同时以新 split-residual-norm 目录重建 `vit_blocks.snnop.mlir`，6 个函数、106/106 唯一 parameter symbol 并通过 verifier。验证：增量 build 成功，CTest 9/9，通过 direct SNNExec round-trip、missing-weight 负例与真实 block importer→fusion→DCE→lowering。未覆盖 spatial pattern、mapping、packing、硬件 lowering、SNNExec 之后的 simulator export。详见 `compiler/docs/SNNEXEC.md`。

- 2026-09-04：完成 model-neuron fusion IR 约定调整。独立 `snn_op.st_bif` 恢复为同 type 的 SSA input/output，importer 下游使用其输出；普通数值 tensor 和 fused tracer element type 由 NIR `metadata/output` dtype 生成（当前 block_00 为 `i8`）。新增 `--dead-neuron-out-eliminate`：严格按 def-use 分别消除死 spike/tracer result，两个都死时删除 fused op；fusion 把函数末端 return 改成最后 fused op 的 `(spike, tracer)`。fused result 的 printer 强制使用独立数字 SSA group（`%0, %1`），不输出 `%0:2`/`%0#0`。目标 block_00 完成 import → verify → fusion → dead-output elimination → verify，CTest 通过；未覆盖 SNNExec、mapping、packing 与硬件 lowering。详见 `compiler/docs/MODEL_NEURON_FUSION.md`。

- 2026-09-04：完成 `snn_op` model-neuron fusion。`st_bif` 现为无结果的神经元参数注释，fusion 前 importer 保留 model 的 i32 数据边；`--model-neuron-fusion` 仅凭 producer Operation mnemonic 和 def-use edge 配对，合并 model/ST-BIF 属性、重连消费者并删除原 pair。新增 `conv2d/linear/q/k/v/z/fc/affine/norm/qk/qkv/residual/rescale/pool` 的 `_stbif` op，均产出 `(tensor<...x!snn.spike<ternary>>, tensor<...xi32>)`。QK/QKV 固定为四个 `(spike,tracer)` 输入并有 type/shape verifier；residual 保留 importer 从 NIR `shifter1/shifter2` 推导的正 `w_main/w_skip`。真实 split-residual-norm block_00 经导入、融合、再 parse/verify 成功，得到 13 个 fused op（3 norm、q/k/v、qk、qkv、z、2 fc、2 residual）；CTest 5/5 通过，`git diff --check` 通过。交付说明：`compiler/docs/MODEL_NEURON_FUSION.md`。未覆盖：SNNExec、spatial pattern、mapping、hardware lowering、weight packing。

- 2026-09-04：完成 split-residual-norm block_00 的语义专用 mnemonic 导出。保留 generic `linear/affine/av` 兼容，新增并注册 `x_wq/x_wk/x_wv/z_wo/fc/norm/qkv`；它们分别复用 linear、affine、AV 的 parameter/shape/head verifier。exporter 以显式 node-name 表选择 `x_wq/x_wk/x_wv/z_wo`、`fc`、`norm`、`qk`、`qkv`、`residual`，不改 SSA 命名、ST-BIF 或参数 source。目标输出不含 generic `linear/affine/av/rescale`。验证：MLIR 23 增量构建成功，CTest 4/4（正例涵盖所有新 mnemonic）通过，block_00 由 `soma-opt --verify-each` 通过，`git diff --check` 通过。

- 2026-09-04：完成 split-residual-norm block_00 的显式 NIR lowering。新增 `snn_op.affine`（weight/bias symbol、axis、time_dim），verifier 检查 input/output shape 一致且参数 kind 正确；importer 仅接受明确支持的 node name，按 `linear/qk/av/residual/affine` 分派，`block_input` 仅为函数参数，未知名称抛出 `ValueError`，完全移除 `weight.ndim == 2` / `rescale(1.0)` 的 op 选择。该 NIR 将阈值置于 `metadata/multiplier`，故保留旧 `node/threshold` 优先级并在缺失时回退读取 multiplier，tracer 逻辑不变。输出 `mlir-soma/output/block_00_split_residual_norm.snnop.mlir`：6 linear、1 qk、1 av、3 affine、2 residual、13 st_bif、0 rescale。验证：MLIR 23 增量构建成功，CTest 4/4（含 affine shape 负例）通过，目标 NIR 单文件导入与 `soma-opt --verify-each` 通过。未覆盖：snn_exec、mapping、weight packing。

- 2026-09-03：`snn_op` 参数已从 `linear/conv2d` 的 URI 字符串解耦为 module-level `snn_op.param` symbol：声明保存 source/kind/shape/dtype，使用侧为必选 `weight=@...` 和可选 `bias=@...`；Param/consumer verifier 覆盖元数据、符号解析和 kind。NIR 单文件及 6-block 目录导入均先收集参数，以 `block/node` 前缀保持 symbol 唯一，不写 tensor 内容；删除 linear/conv2d 的 `spike_encoding` 与 `st_bif` 的冗余 `neuron_model`。验证：MLIR 23 构建成功，CTest 3/3（valid weight/bias、无 bias、缺失 symbol、weight→bias kind）通过，单文件和目录输出均由 `soma-opt --verify-each` 通过，目录无重复 parameter symbol。未覆盖：weight packing、NPZ、`snn_exec` lowering。

- 2026-09-03：完成 `mlir-soma` 的 `snn_op` V1。新增共享 `!snn.spike<binary|ternary>`、TableGen 定义的 `conv2d/linear/qk/av/residual/pool/rescale/lif/st_bif`、关键 shape/spike/neuron verifier 和 `--snnop-canonicalize`（连续 rescale 合并）。新增 NIR 1.0.8 HDF5 block 导入器，权重保留为 `nir://...#/node/...` resource key，不嵌入 tensor。指定目录的 6 个 ViT blocks 已归并输出为单一 `mlir-soma/output/vit_blocks.snnop.mlir`，其中包含 `linear → st_bif`、`qk → st_bif`、`av → st_bif`。验证：`cmake -S compiler/mlir-soma -B compiler/mlir-soma/build -G Ninja -DMLIR_DIR=/home/ydy/compiler/install/lib/cmake/mlir`、`cmake --build compiler/mlir-soma/build --target soma-opt -j2`、`soma-opt --verify-each --snnop-canonicalize test/snnop_smoke.mlir`、以及真实 blocks 导入后的 `soma-opt --verify-each` 均成功。未覆盖：仅实现 V1 import/verify，未实现 snn_exec lowering、weight packing 与 mapping/hardware/event execution。
